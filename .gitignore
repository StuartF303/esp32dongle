# Scratch dumps and analysis artefacts — the canonical images live in
# backup/factory_release/, which is deliberately NOT ignored.
*.strings
part_*.bin
sub_*.bin
fix_*.bin
appdump.bin
apphdr.bin

# Per-machine Claude Code permissions. The harness override in .claude/hooks/ IS
# tracked — it records a project decision — but local permissions are not.
.claude/settings.local.json
